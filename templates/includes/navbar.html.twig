			
    <link rel="stylesheet" type="text/css" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css">

    <style>

      #fat {
            color:white;
            {# text-transform:lowercase !important; #}
      }
        .fa-circle-user:before, .fa-user-circle:before {
            font-size: 30px !important;
            padding-right: 4px;
        }
        a{
            font-size: 18px;
            color: black;
            text-decoration: none !important;
        }
        a:hover {
            color: white !important;
        }
        a:hover {
            color: white !important;
            text-decoration: none !important;
        }
        .fa-user:before {
            padding-right: 4px !important;
            font-size: 17px;
        }
        .ft{
          font-size:12px;
        }
    
    </style>


      <nav class="navbar navbar-expand-lg clr text-light" style="background-color: #5f9ea0 !important; margin-bottom: 2.7rem!important;">
          <a class="navbar-brand logo text-light" href="{{path('balance')}}">
            <i class="fa-solid fa-align-left"></i>
            <span class="text-light">BALANCE</span>
          </a>
          <a class="app-sidebar__toggle" href="#" data-toggle="sidebar" aria-label="Hide Sidebar"></a>
          <ul class="navbar-nav mr-auto"></ul>
          <ul class="navbar-nav navbar-right">
            <li class="app-search">
              <li class="dropdown"><a class="app-nav__item " href="" data-toggle="dropdown" data-bs-toggle="dropdown" aria-label="Open Profile Menu">
                <i class="fa fa-user fa-lg" id="fat"></i>
                {# {{ app.user.username }} #}
                {# <i class="fa-solid fa-circle-user " id="fat"></i>{{ app.user.username }} #}
                </a>
                <ul class="dropdown-menu settings-menu dropdown-menu-right">
                  <li><a class="dropdown-item ft" href="{{path('app_side')}}"><i class="fa fa-cog fa-lg"></i> Réglages</a></li>
                  <li><a class="dropdown-item ft" href="{{path('app_side')}}"><i class="fa fa-user fa-lg"></i> Profil</a></li>
                  <li><a class="dropdown-item ft" href="{{path('app_login')}}"><i class="fa fa-sign-out fa-lg"></i> Se déconnecter</a></li>
                </ul>
            </li>
          </ul>
      </nav>

      <script src="{{asset('assetss/js/navbar.js')}}"></script>